strands-agents>=1.0.0
strands-agents[a2a]>=1.0.0
strands-agents-tools>=0.2.0
boto3>=1.35.0
pyyaml>=6.0
pydantic>=2.0.0
requests>=2.31.0
httpx>=0.27.0
magika>=0.5.0
tree-sitter>=0.21.0
tree-sitter-languages>=1.10.0
networkx>=3.2
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-http>=1.25.0
opentelemetry-instrumentation-botocore>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0

# ─── Baseline Dev Tools (ADR-038 Wave 4: Defense in Depth) ──────────
# These are always available in the container regardless of Step 7.7
# runtime provisioning. Ensures pre-flight passes for common Python tasks
# even if dependency_resolver fails silently (H2 scenario).
pytest>=8.0.0
pytest-cov>=5.0.0
pip-tools>=7.4.0
ruff>=0.4.0

# ── Dev tooling baseline (ADR-038 Wave 4: Dependency Provisioning) ──
# These are always available in the container for execution tasks.
# Additional tools are provisioned at runtime by dependency_resolver.py.
pytest>=8.0.0
pytest-cov>=5.0.0
pip-tools>=7.4.0
ruff>=0.4.0
